apiVersion: v1
kind: List
items:
  - apiVersion: kubevirt.io/v1
    kind: VirtualMachine
    metadata:
      name: cirros-multipvc-test
      namespace: cirros-multipvc-test
    spec:
      dataVolumeTemplates:
        - apiVersion: cdi.kubevirt.io/v1beta1
          kind: DataVolume
          metadata:
            name: cirros-multipvc-test-disk
          spec:
            sourceRef:
              kind: DataSource
              name: cirros
              namespace: openshift-virtualization-os-images
            storage:
              resources:
                requests:
                  storage: 150Mi
              storageClassName: test-sc-wffc
        - apiVersion: cdi.kubevirt.io/v1beta1
          kind: DataVolume
          metadata:
            name: cirros-multipvc-test-datadisk
          spec:
            source:
              blank: {}
            storage:
              resources:
                requests:
                  storage: 100Mi
              storageClassName: test-sc-wffc
      running: true
      template:
        metadata:
          annotations:
            vm.kubevirt.io/flavor: tiny
        spec:
          domain:
            devices:
              disks:
              - disk:
                  bus: virtio
                name: rootdisk
              - disk:
                  bus: virtio
                name: datadisk
            firmware:
              bootloader:
                efi:
                  secureBoot: false
            resources:
              requests:
                cpu: 1
                memory: 256Mi
          volumes:
          - name: rootdisk
            persistentVolumeClaim:
              claimName: cirros-multipvc-test-disk
          - name: datadisk
            persistentVolumeClaim:
              claimName: cirros-multipvc-test-datadisk
